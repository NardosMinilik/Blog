# Simple Modular Blog

### Setup Instructions
1. Keep all 4 files (`index.html`, `style.css`, `script.js`, `data.js`) in the same folder.
2. Open `index.html` in your browser.

### Managing Content
To add a new blog post, simply open **data.js** and add a new entry to the `blogPosts` array. The website will update automatically!
